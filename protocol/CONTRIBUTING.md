# Contributing

Use a Conventional Commits pull-request title. Update the canonical protocol
schema and regenerate every checked-in C and Python binding together; do not
hand-edit generated output or copy command identifiers into consumers. Run the
validation commands in `README.md` before opening a pull request. Protocol work
is GPL-2.0-or-later under the monorepo's root `LICENSE.md`.
