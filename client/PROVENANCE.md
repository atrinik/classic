# Provenance and ownership

This repository was extracted from `client/` and its client-specific build
tools in [`atrinik/atrinik`](https://github.com/atrinik/atrinik) at source
commit `f00d892c4`. Its filtered history retains changes to those paths.

The extraction does not relicense the client. `COPYING`, source-file notices,
`textures/LICENSE`, and font notices remain authoritative. The Atrinik
maintainers own client builds, packages, dependency locks, security fixes, and
semantic-version releases.

